# Detect scene and image extension changes and copy / merge metadata

Looks for scene and image files that have changed file extensions (for example from converting them to a more optimal format). 

Requires both old and new file to be in database, so run a scan before running this plugin. 

If the old file has been renamed to the new name, do not run a clean-up before completing migrating all data. 
